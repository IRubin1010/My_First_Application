<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".controller.MainActivity"
    android:background="@drawable/postman"

    >

    <Button
        android:id="@+id/AddParcelActivityBtn"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="Add package"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/HistoryParcelActivityBtn"
        app:layout_constraintStart_toStartOf="@+id/HistoryParcelActivityBtn"
        app:layout_constraintTop_toBottomOf="@+id/HistoryParcelActivityBtn"
        app:layout_constraintVertical_bias="0.283" />

    <Button
        android:id="@+id/HistoryParcelActivityBtn"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="Package History"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/GPSbtn"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="@+id/GPSbtn"
        app:layout_constraintTop_toBottomOf="@+id/GPSbtn"
        app:layout_constraintVertical_bias="0.208" />

    <Button
        android:id="@+id/Addbtn"
        android:layout_width="200dp"
        android:layout_height="63dp"
        android:text="Add person to social network"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/AddParcelActivityBtn"
        app:layout_constraintStart_toStartOf="@+id/AddParcelActivityBtn"
        app:layout_constraintTop_toBottomOf="@+id/AddParcelActivityBtn"
        app:layout_constraintVertical_bias="0.495" />

    <Button
        android:id="@+id/GPSbtn"
        android:layout_width="200dp"
        android:layout_height="48dp"
        android:layout_marginTop="84dp"
        android:text="Check GPS connection"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>